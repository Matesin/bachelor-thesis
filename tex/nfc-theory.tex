\chap[nfc-theory] NFC

This chapter introduces the principles of  {\sbf Near Field Communication (NFC)} relevant to the implementation presented in this thesis. Since NFC covers a broad set of radio technologies, operating modes, and higher-layer protocols, the discussion is {\sbf limited} primarily to {\sbf NFC-A}, {\sbf Card Emulation}, {\sbf ISO-DEP}, and {\sbf APDU}-based {\sbf communication}. Other notable technologies within the NFC ecosystem (as shown in Figure~\ref[img-nfc-stack]) are only briefly discussed to provide context and completeness.

\sec[nfc-intro] Introduction to NFC

NFC is a short-range wireless communication technology derived from radio-frequency identification (RFID) systems. It enables {\sbf bidirectional data exchange} between devices in immediate or close proximity, typically within a range of up to 10~cm, with reliable operation occurring at distances of approximately 4~cm~\cite[rfid-nfc-introduction]. It is standardized primarily through ISO/IEC specifications and NFC Forum\urlnote{https://nfc-forum.org} specifications. The key specifications include ISO/IEC~14443, which defines proximity card communication (used in NFC-A and NFC-B), and ISO/IEC~18092, which specifies peer-to-peer communication. In addition, the NFC Forum defines higher-level protocols and data formats to ensure interoperability across devices.

\nl NFC operates at a {\sbf carrier frequency (fc)} of {\sbf 13.56~MHz} and is based on {\sbf inductive coupling} between {\sbf loop antennas}. In this mechanism, defined in \cite[iso14443-2], an active device generates an electromagnetic field, while a passive device modulates this field to transmit data. The technology supports three primary operating modes: {\sbf reader/writer mode, card emulation (CE) mode, and peer-to-peer mode}. From the perspective of this thesis, the {\sbf CE mode} is of particular importance, as it allows the device to behave as a contactless smart card and participate in ISO-DEP communication with an external reader.

\sec[nfc-device-types] Device Types

NFC devices fall into three categories: {\sbf active} devices (proximity coupling devices or PCDs), {\sbf passive} devices (proximity integrated circuit cards or {\sbf PICCs}), and {\sbf hybrid} devices (proximity extended devices or PXDs).

\nl {\sbf Active devices}, or proximity coupling devices ({\sbf PCDs}), are equipped with their own power source and generate an electromagnetic field, enabling them to initiate and control NFC communication.

\nl In contrast, {\sbf passive devices}, commonly implemented as {\sbf dynamic NFC tags}, do {\sbf not require an internal power supply}. Instead, they rely on the energy harvested from the electromagnetic field generated by an active device {\sbf (Energy Harvesting)}. {\sbf Communication} from the passive side is achieved through {\sbf load modulation}, i.e., by varying the impedance of the antenna circuit, which in turn modulates the carrier field. The characteristics of PICCs are defined in~\cite[iso14443-1]. 

\nl {\sbf Hybrid devices}, or proximity extended devices ({\sbf PXDs}), can operate in either PCD or PICC mode. Such devices may switch between modes automatically depending on the communication context, or allow {\sbf manual selection} by the user.

\sec[nfc-device-roles] Device Roles

In NFC communication, two roles are distinguished according to which device initiates the interaction: the {\sbf initiator} and the {\sbf target}. These roles are defined at the protocol level and are independent of the specific hardware capabilities of the devices.

\begitems
* {\sbf Initiator}
\nl The device that actively starts the communication and generates the initial RF field. It is responsible for polling and controlling the communication sequence, as is typically the case for an NFC reader.

* {\sbf Target}
\nl The device that responds to the initiator. Depending on the communication mode, it can passively modulate the initiator’s field or actively generate its own signal. Typical examples include contactless smart cards, NFC tags, or peer devices. 
\enditems
It is important to distinguish these roles from the physical ability to generate an electromagnetic field. In passive communication mode, the initiator generates the RF field. In active communication mode, both devices are capable of generating their own RF field during their respective transmission phases.

\nl NFC defines two communication modes based on how the electromagnetic field is generated and how the devices are powered: {\sbf active mode} and {\sbf passive mode}.

\begitems

* {\sbf Active Mode}
\nl In active mode, both the initiator and the target are equipped with their own power sources and are capable of generating an RF field. Communication is performed through alternating transmission, where each device generates its own carrier field during its transmission phase. This mode is used primarily in peer-to-peer communication scenarios.

* {\sbf Passive Mode}
\nl In passive mode, only the initiator generates the RF field. The target may not have its own power source and may utilize energy harvesting. Data transmission from the target is achieved through load modulation of the existing carrier field. This mode is characteristic of typical NFC-A and NFC-B interactions, such as communication with contactless smart cards and NFC tags.
\enditems

\sec [nfc-operating-modes] Operating Modes

The NFC Forum defines {\sbf three} primary {\sbf operating modes} that determine the role and behavior of a device within a communication session.

\begitems

* {\sbf Reader/Writer Mode}
\nl In the reader/writer mode, the PCD operates as an initiator and interacts with passive PICCs. This mode is commonly used for applications such as reading NDEF records, accessing contactless cards, or configuring embedded devices. 

* {\sbf Card Emulation Mode}
\nl In CE mode, the PICC behaves as a contactless smart card and acts as a target. It responds to commands issued by the PCD, typically using the ISO-DEP protocol. This mode is widely used in applications such as contactless payments, access control, and mobile wallets. Its main advantage is the possibility of implementing a more complex state machine while retaining the ability to utilize energy harvesting. This mode has two variants of implementation. It can either be implemented on a general-purpose MCU or using a dedicated {\sbf secure element (SE)} hardware, depending on the use case. Commercial NFC-capable security keys commonly rely on secure-element-based designs, as indicated, for example, by the Yubikey tech manual~\cite[yubikey-manual] or by the Google Titan documentation~\cite[titan-key-documentation].

* {\sbf Peer-to-Peer Mode} 
\nl The peer-to-peer mode enables half-duplex communication between two NFC-enabled devices, where both devices can alternately assume the roles of initiator and target. Communication in this mode is defined by \cite[iso18092] and the NFC Forum Logical Link Control Protocol (LLCP). It is primarily used for data exchange scenarios such as device pairing or file transfer, although its practical use has declined in favor of higher-throughput wireless technologies.

\enditems
These operating modes build upon the previously described concepts of initiator and target roles, as well as active and passive communication modes. Their combination defines the functional behavior of NFC devices in real-world applications. 

\sec[nfc-rf-technologies] NFC RF Technologies
\secc[nfc-a] NFC-A

{\sbf NFC-A} is based on ISO/IEC 14443 Type A. In typical NFC-A communication, initialization and anticollision are performed at 106 kbit/s (fc/128). In theory, the specification allows speeds up to 27.12 Mbit/s (2fc) after activation.

\nl {\sbf Encoding and Modulation:} Both NFC-A and NFC-B use {\sbf asymmetric} transaction methods. At 106 kbit/s, NFC-A employs {\sbf Modified Miller encoding} in the reader-to-card (PCD to PICC) direction, where logical bits are represented by pulse-position transitions in signal amplitude. The card-to-reader (PICC to PCD) direction uses {\sbf Manchester encoding} with a subcarrier at fc/16. These encodings improve robustness against noise and enable reliable clock synchronization.

\nl The encoded signal modulates the RF carrier using different schemes depending
on direction and data rate:
\begitems
* {\sbf 106 kbit/s to fc/16 (PCD to PICC):} 100\% Amplitude Shift Keying (ASK) modulation with Modified Miller encoding where the carrier is fully suppressed during pause intervals.
* {\sbf 106 kbit/s (PICC to PCD):} OOK (On-Off Keying) modulated subcarrier at fc/16 with Manchester encoding via load modulation.
* {\sbf fc/8 to fc/2}: Same modulation as NFC-B (see below).
* {\sbf 3fc/4 to 2fc}: Phase Shift Keying (PSK) modulation, where the carrier phase shifts to encode information, reducing sensitivity to amplitude noise at higher speeds. ASK is retained for the PCD to PICC direction.
\enditems

\midinsert
\clabel[img-nfc-modulation]{NFC Modulation}
\picheight = 60mm \cinspic ../images/nfc-modulation.png
\caption /f Examples of NFC-A and NFC-B modulation \cite[iso14443-2]
\endinsert

\secc[nfc-b] NFC-B

{\sbf NFC-B} is based on ISO/IEC 14443 Type B. Initialization and anticollision operate at 106 kbit/s, with a maximum data exchange speed of 6.78 Mbit/s (fc/2), lower than NFC-A's maximum.

\nl {\sbf Encoding and Modulation:} NFC-B uses {\sbf NRZ-L (Non-Return-to-Zero Level)} encoding, where high and low amplitude levels directly represent logical 1 and 0 without returning to zero between bits.

\begitems

* {\sbf PCD to PICC}: 10\% ASK modulation. The carrier amplitude drops to approximately 90\% of the amplitude to represent a logical 0, and remains at full amplitude for logical 1. The low modulation index keeps the carrier stable, reducing RF noise pickup.
* {\sbf PICC to PCD}: Binary Phase Shift Keying (BPSK) modulation of a subcarrier (fc/16), which allows the card to respond without being overwhelmed by the reader's transmitted signal. The initial phase of the subcarrier represents logical 1 and a 180 degree shift represents a logical 0.
\enditems
The lower modulation index results in {\sbf improved robustness} against electromagnetic interference. This means that NFC-B is more suitable for applications requiring high reliability in noisy environments.

\secc[nfc-f] NFC-F

{\sbf NFC-F} is based on the JIS X 6319-4 specification and was originally developed by Sony under the commercial name FeliCa. Unlike NFC-A and NFC-B, NFC-F is not strictly derived from the ISO/IEC 14443 family, but instead follows a different protocol structure defined in Japanese industrial standards.

\nl A key distinguishing feature of NFC-F is its higher data rates, typically 212 kbit/s or 424 kbit/s, which makes it suitable for latency-sensitive applications. The technology is used primarily in Japan, particularly in transportation systems, contactless payment infrastructures, and access control solutions.

\nl However, due to its regional focus and protocol differences, NFC-F is {\sbf not universally supported by all NFC readers}. Consequently, its applicability in general-purpose NFC implementations is limited and it is mentioned in this overview purely for completeness.

\midinsert
\clabel[img-nfc-stack]{NFC Protocol Stack}
\picheight = 80mm \cinspic ../images/NFC_Protocol_Stack.png
\caption /f NFC Protocol Stack~\cite[nfc-protocol-stack]
\endinsert

\sec[nfc-a-state-machine] NFC-A State Machine

NFC-A anticollision and activation sequence has a defined state machine behavior, respecting~\cite[iso14443-3]. This state machine must be respected for the correct initialization of the communication and protocol flow, as illustrated in Figure \ref[img-nfc-init-flow]. 

\nl A PCD can use the following {\sbf commands} during the anticollision sequence an NFC-A PICC:

\begitems 
* {\sbf REQA} (REQuest, Type A) 
\nl Broadcast command used to poll for any NFC-A cards in the field. Only cards in IDLE state respond.
* {\sbf WUPA} (Wake-UP, Type A) 
\nl Similar to REQA, but also wakes cards in HALT state. Cards in both IDLE and HALT states respond.
* {\sbf ANTICOLLISION}
\nl Used to resolve collisions when multiple cards respond simultaneously, by iteratively isolating individual UIDs.
* {\sbf SELECT} 
\nl Selects a specific card by its full UID, moving it into the ACTIVE (or ACTIVE$^*$) state.
* {\sbf HLTA} 
\nl Instructs the active card to enter the HALT state, suspending it until a WUPA command is received.
\enditems

The NFC-A state machine has the following {\sbf states}:

\begitems

* {\sbf POWER-OFF}
\nl The PICC is not in an RF field exceeding Hmin. Communication is not possible. The transition to IDLE occurs when the RF field strength exceeds Hmin.

* {\sbf IDLE}
\nl The PICC is powered by the RF field but has not yet been addressed. It responds only to REQA or WUPA commands. A valid REQA or WUPA transitions the card to READY.

* {\sbf READY}
\nl The PICC has responded to REQA or WUPA. The anticollision loop begins here: the PCD issues ANTICOLLISION commands to isolate the card's UID, then a SELECT command to uniquely address it. A successful SELECT transitions the card to ACTIVE. A failed SELECT or collision returns the card to IDLE.

* {\sbf READY$^*$}
\nl Functionally identical to READY, but entered exclusively from HALT via WUPA. A successful SELECT transitions the card to ACTIVE$^*$.

* {\sbf ACTIVE}
\nl The PICC has been selected by its full UID and is ready to communicate. If the card supports ISO-DEP~\ref[nfc-iso-dep], it awaits a RATS command to enter the PROTOCOL state. Otherwise, it continues with its proprietary protocol. An HLTA command transitions the card to HALT.

* {\sbf ACTIVE$^*$}
\nl Functionally identical to ACTIVE, but entered from READY$^*$. The distinction allows the PCD to identify cards that were reactivated from HALT, which may be relevant for session management. A HLTA command returns the card to HALT.

* {\sbf HALT}
\nl The PICC is suspended and ignores all commands except WUPA. This allows the PCD to selectively wake specific cards in a multi-card scenario. A valid WUPA transitions the card to READY$^*$.

* {\sbf PROTOCOL}
\nl The PICC operates under the ISO-DEP protocol, initiated by a successful RATS/ATS exchange. All subsequent communication follows the negotiated parameters (FWT, FSC, etc.). The card remains in this state until the RF field is removed or a DESELECT command is issued, returning it to HALT or IDLE, respectively.

\enditems

\midinsert
\clabel[img-nfc-init-flow]{NFC Initialization Flow}
\picheight = 60mm \cinspic ../images/nfc-init-flow.png
\caption /f PlantUML diagram of the NFC initialization flow.
\endinsert

\secc[nfc-a-anticollision] NFC-A Initialization and Anticollision

NFC-A uses a {\sbf bit-frame anticollision} method based on binary tree traversal. Since multiple PICCs may respond simultaneously to a REQA or WUPA command, their signals can overlap and corrupt each other, this is called a {\sbf collision}. The anticollision loop resolves collisions iteratively by narrowing down the UID of each card in the field. This overview {\sbf focuses solely on the NFC-A procedures}, although the standard also specifies behavior for hybrid tags supporting both the NFC-A and NFC-B types.

\nl The commands defined in this subsection are later used in Chapter~\ref[evaluation] to ensure correct operation of the NFC frontend.

\seccc[nfc-a-polling] Polling

Before sending a REQA command, the PCD must present an unmodulated RF field for a minimum of {\sbf 5.1 ms}. This ensures that passive PICCs, which rely on energy harvesting, are sufficiently powered before they are expected to respond. The PCD then broadcasts REQA to poll all Type A PICCs in IDLE state, or WUPA to additionally wake PICCs in HALT state.

\seccc[nfc-a-atqa] Answer To reQuest (ATQA)

Upon receiving a valid REQA or WUPA, all eligible PICCs respond simultaneously with an {\sbf ATQA (Answer To reQuest, Type A)}, a 2-byte frame encoding (see Table \ref[table-atqa]:
\begitems

* {\sbf Bit frame anticollision indicator} - which bit position is used as the anticollision entry point in the binary tree traversal
* {\sbf UID size indicator} - whether the UID is 4, 7 or 10 bytes, determining how many cascade levels the anticollision loop must traverse.
\enditems

\leavevmode

\nl If multiple PICCs respond in the same slot, their ATQA frames collide at the bit level. The PCD detects this and proceeds with the anticollision loop to isolate individual cards.

\midinsert \clabel[table-atqa]{ATQA message structure}
\ctable{c|c}{
\hfil Byte 1 & Byte 2 \crl \tskip5pt
 Bit frame anticollision indicator & UID size indicator \crl
}
\caption/t ATQA structure.
\endinsert

\seccc[nfc-a-uid] UID Structure and Cascade Levels
NFC-A PICCs have UIDs of three possible lengths, each requiring a different number of cascade levels to fully resolve:
\begitems
* {\sbf Single (4 bytes)} - resolved in one cascade level (CL1)
* {\sbf Double (7 bytes)} - resolved across two cascade levels (CL1, CL2)
* {\sbf Triple (10 bytes)} - resolved across three cascade levels (CL1, CL2, CL3)
\enditems
Each cascade level transmits 4 bytes of UID data at a time. When more levels remain, the first byte of the current level is set to the {\sbf Cascade Tag} (CT, value \code{0x88}), signaling to the PCD that the UID continues in a subsequent cascade level.

\seccc[nfc-a-loop] Anticollision Loop

The following sequence repeats for each required cascade level. Note that {\sbf bits are indexed from 1}.

\begitems

* The PCD sends an {\sbf ANTICOLLISION} command (see Table \ref[table-anticollision]) specifying the current cascade level (CL1 (0x93), CL2 (0x95), or CL3 (0x97)) and an {\sbf NVB (Number of Valid Bits)} field indicating how many leading bits of the UID are already known. Initially NVB = 0, meaning no bits are known yet.

* All PICCs in READY (or READY$^*$) state whose UID matches the known prefix respond with their remaining UID bits.

* If exactly one PICC responds, no collision occurred and the PCD has resolved the full UID fragment for this level.

* If multiple PICCs respond simultaneously, a collision is detected at the first bit position where responses differ. The PCD appends either a {\sbf 0} or {\sbf 1} at the collision point, increments NVB, and reissues the ANTICOLLISION command - effectively traversing one branch of a binary tree. This repeats until a single PICC is isolated.

* Once the full 4-byte UID fragment for the current level is known, the PCD issues a {\sbf SELECT} command containing the complete fragment and a {\sbf BCC (Block Check Character)}, which is the XOR of the four UID bytes, used for integrity verification.

* The addressed PICC responds with a {\sbf SAK (Select AcKnowledge)}, confirming selection (Table \ref[table-anticollision]). The SAK also indicates whether further cascade levels are required (bit 2 of SAK set) or whether the full UID has been resolved (bit 2 cleared).

\enditems

\midinsert \clabel[table-anticollision]{ANTICOLLISION command structure}
\ctable{c|c|c|c|c|c|c|c}{
\hfil SEL & \multispan4 NVB \vrule & UID CLn & BCC & E\crl \tskip5pt
 CL & 0 & byte cnt. & 0 & bit cnt. & 0--4 bytes (NVB) & & \crl
}
\caption/t ANTICOLLISION command structure. 
\endinsert

\midinsert \clabel[table-sak-bits]{SAK bit interpretation}
\ctable{c|c|c}{
\hfil Bit 2 & Bit 5 & Meaning \crl \tskip5pt
 1 & -- & UID incomplete, further cascade levels required \crl \tskip5pt
 0 & 0  & UID complete, proprietary protocol \crl \tskip5pt
 0 & 1  & UID complete, ISO-DEP supported \crl
}
\caption/t SAK bit~2 and bit~5 interpretation.
\endinsert

\leavevmode

\nl Once all cascade levels are resolved and the final SAK confirms a complete UID, the PICC transitions to {\sbf ACTIVE} state. If the SAK additionally indicates ISO-DEP support (bit 5 set), the PCD may proceed with a RATS command to initiate the ISO-DEP protocol. Otherwise, the PICC continues with its own proprietary protocol.

\sec[nfc-protocols] Communication Protocols

NFC communication is built on a layered protocol stack that defines how data is exchanged between devices after the initial activation phase. At the transport and higher communication layers, two primary protocols are used: ISO-DEP and NFC-DEP. In addition, several auxiliary protocol layers defined by the NFC Forum provide standardized data exchange formats.

\secc[nfc-iso-dep] ISO-DEP

The {\sbf ISO-DEP} (ISO Data Exchange Protocol), specified in~\cite[iso14443-4], defines a transport layer for reliable data exchange between a PCD and a PICC. It extends the lower NFC-A/B protocols by introducing a {\sbf block-oriented, half-duplex communication mechanism} that enables structured and robust transmission of application data. This is the {\sbf most significant aspect} of the NFC technology for the subject of this thesis.

\seccc[nfc-iso-dep-activation] Activation and Parameter Negotiation

Before data exchange can begin, ISO-DEP requires an {\sbf activation phase} in which the initiator selects the target and negotiates communication parameters. This is performed using the {\sbf RATS} (Request for Answer To Select) and {\sbf ATS} (Answer To Select) mechanism.

\nl First, the reader sends a RATS command (see Table \ref[table-rats]). RATS is a 2-byte command consisting of a fixed command byte (E0) and a parameter proposal byte (PP).

\nl RATS consists of a fixed command byte \code{0xE0} followed by a parameter byte. The upper nibble of the parameter byte encodes the Frame Size for Device Integer (FSDI); the conversions from FSDI to FSD are shown in Table \ref[table-fsdi-conversion]. The lower nibble of RATS encodes the Card Identifier (CID), which logically addresses a specific card when multiple cards are in the field. This value remains assigned to the PICC until it leaves the field. CID=15 is reserved. 

\midinsert \clabel[table-fsdi-conversion]{FSDI to FSC conversion}
\ctable{c|c|c|c|c|c|c|c}{
\hfil FSDI & 0 & 1 & 2 & 3 & 4 & 5 & 6\crl \tskip5pt
 FSD (bytes) & 16 & 24 & 32 & 40 & 48 & 64 & 96 \crl \tskip5pt
 FSDI & 7 & 8 & 9 & A & B & C & D--F \crl \tskip5pt
 FSD (bytes)& 128 & 256 & 512 & 1024 & 2048 & 4096 & RFU \crl
}
\caption/t FSDI to FSD conversion table
\endinsert

\midinsert \clabel[table-rats]{RATS message structure}
\ctable{c|c|c|c|c}{
\hfil E0 & \multispan2 Parameter Proposal \vrule & CRC1 & CRC2 \crl \tskip5pt
1B  & FSDI & CID & 1B & 1B \crl
}
\caption/t RATS structure.
\endinsert

\leavevmode

\nl The ATS response consists of a total length value (TL), format byte (T0), optional interface bytes (TA, TB, TC) and optional Historical Bytes (up to 15 bytes). These fields define key communication parameters, including timing constraints and frame sizes. All blocks are terminated by a two-byte CRC. This is illustrated in Table \ref[table-ats].

\midinsert \clabel[table-ats]{ATS message structure}
\ctable{c|c|c|c|c|c|c|c|c}{
\hfil TL & T0 & TA(1) & TB(1) & TC(1) & T1 & $T_k$ & CRC1 & CRC2 \crl \tskip5pt
1B  & 1B & \multispan3 interface \vrule & 1B & HB & 1B & 1B\crl
}
\caption/t ATS structure.
\endinsert

\leavevmode 

\nl The interface bytes can contain these values: 

\begitems

* {\sbf Frame Size (FSC/FSD)}
\nl Defined by the FSCI field in T0, it specifies the maximum payload size of a single frame for the PICC (FSC) and the PCD (FSD), determining whether chaining is required.

* {\sbf Frame Waiting Time (FWT)} 
\nl  Specifies the maximum time the initiator waits for a response from the target. It is derived from the Frame Waiting Time Integer (FWI) in TB(1) using the formula: 
$$
FWT = {256 \times 16 \over f_c} \times 2^{FWI}
$$

* {\sbf Frame Guard Time (FGT)} 
\nl Derived from the Guard Time Integer (GTI) in TB(1), FGT specifies the minimal delay that must elapse before next transaction. This prevents receiver saturation and ensures reliable synchronization.

* {\sbf Device Identifier (CID)}
\nl When there are multiple cards present in the field, the initiator can use CID (0–14), specified in TC(1), to address a specific card. If not used, the CID is typically set to 0 or completely omitted.

* {\sbf Node Address Support (NAD)}
\nl TC(1) may also indicate support for the optional NAD mechanism used for logical node addressing at higher protocol layers. In practice, NAD is rarely implemented in modern NFC devices.

* {\sbf Historical Bytes (HB)}
\nl Optional data included in the ATS; they are to be interpreted according to the card manufacturer's specifications, and they may provide additional information about the capabilities of the card if present. Contents of HB are further specified in \cite[iso7816-4].
\enditems

\seccc[nfc--dep-data] Data Exchange Blocks

Communication in {\sbf ISO-DEP} is organized into protocol data units called {\sbf blocks}, which are exchanged between the initiator and the target. There are two types of blocks: {\sbf Standard} and {\sbf Extended} blocks.

\nl The general structure of ISO-DEP blocks is illustrated in Tables~\ref[table-id-st-block] and \ref[table-id-ext-block]. Note that fields in brackets (\code{[]}) are optional.

\midinsert \clabel[table-id-st-block]{General ISO-DEP Standard Block structure}
\ctable{c|c|c|c|c|c}{
\multispan3 Prologue \vrule & Information & \multispan2 Epilogue \crl \tskip5pt
PCB & [CID] & [NAD] & [INF] & \multispan2 ECD \crl
1B & 1B &1B & application data & CRC1 & CRC2 \crl
}
\caption/t Structure of a Standard ISO-DEP block
\endinsert

\midinsert \clabel[table-id-ext-block]{General ISO-DEP Extended Block structure}
\ctable{c|c|c|c|c|c}{
Length & \multispan3 Prologue \vrule & Information & Epilogue \crl \tskip5pt
LEN & PCB & [CID] & [NAD] & [INF] & ECD \crl
2B & 1B & 1B &1B & application data & CRC (4B) \crl
}
\caption/t Structure of an Extended ISO-DEP block
\endinsert

\leavevmode

\nl Each block begins with a mandatory one-byte {\sbf PCB (Protocol Control Byte)}, which identifies the block type and encodes protocol control information. Depending on the block type, the PCB may contain information such as block numbering, chaining indication, acknowledgment status, or supervisory commands. Depending on the negotiated protocol configuration, the blocks can also contain optional {\sbf CID} and {\sbf NAD}. I-Blocks and some S-Blocks may also include an {\sbf INF} field that contains application or supervisory data. 

\nl Based on the two leftmost bits of PCB (see Table~\ref[table-isodep-pcb]), we recognize three types of ISO-DEP blocks:
\begitems
* {\sbf I-Blocks} (Information blocks)
\nl Used to transport application data (including APDUs), these are fragmented into I-Blocks and reassembled upon reception.

* {\sbf R-Blocks} (Receive Ready blocks)
\nl Used for acknowledgments and retransmission control.

* {\sbf S-Blocks} (Supervisory blocks)
\nl Used for protocol management and control operations, such as waiting time extension (WTX) or deselection (transaction termination).
\enditems

\midinsert \clabel[table-isodep-pcb]{ISO-DEP PCB meaning}
\ctable{c|c|c}{
b8 & b7 & Block Type \crl \tskip5pt
0 & 0 & I-Block \crl
0 & 1 & RFU     \crl
1 & 0 & R-Block \crl
1 & 1 & S-Block \crl
}
\caption/t ISO-DEP block type encoding
\endinsert

\leavevmode

\nl The S-Blocks with WTX are especially important for blocking operations like cryptographic calculations that are an essential part of LionKey. It is important to handle WTX in time in order to avoid timeouts.
\seccc[nfc-apdu] APDU

ISO-DEP serves as the transport layer for {\sbf Application Protocol Data Units (APDUs)}, which implement a command–response communication model. APDUs are a standardized data format used in smart card systems for structured information exchange defined by ISO/IEC 7816-4.

\nl In this model, the initiator sends a {\sbf command APDU (C-APDU)}, and the target responds with a {\sbf response APDU (R-APDU)}. This abstraction allows higher-level protocols, including smart card applications and CTAP over NFC, to operate independently of the underlying transmission mechanism while relying on ISO-DEP for reliable delivery.

\nl A C-APDU consists of a {\sbf header} (CLA, INS, P1, P2) and an {\sbf optional payload} defined by the fields Lc (length of command data) and Le (expected length of response data).

\nl APDUs can be encoded in short or extended format. In the short format, the length fields are limited to one byte, allowing payloads of up to 255 bytes. The extended format uses two-byte length fields, enabling payloads of up to 65,535 bytes.

\nl In the short format, the C-APDU (see Table~\ref[table-apdu-short-layout]) consists of the mentioned 4-byte header followed by the optional fields Lc, Data (0–255 bytes) and Le, where both Lc and Le are encoded using a single byte. 

\nl In the extended format (see Table~\ref[table-apdu-extended-layout]), the presence of the leading \code{0x00} byte indicates extended length encoding, followed by two-byte fields for Lc and Le. The exact position of Le depends on the APDU case.

\midinsert
\clabel[table-apdu-short-layout]{Short C-APDU byte layout}
\ctable{c|c|c|c|c|c|c}{
\hfil CLA & INS & P1 & P2 & [Lc] & [Data] & [Le] \crl \tskip5pt
1 byte & 1 byte & 1 byte & 1 byte & 0/1 & 0--255 & 0/1 \crl
}
\caption/t Byte-level structure of short C-APDUs.
\endinsert

\midinsert
\clabel[table-apdu-extended-layout]{Extended C-APDU byte layout}
\ctable{c|c|c|c|c|c|c|c}{
\hfil CLA & INS & P1 & P2 & 00 & Lc(Hi) & Lc(Lo) & Data/Le \crl \tskip5pt
1B & 1B & 1B & 1B & ext. marker & \multispan2 length \vrule & variable \crl
}
\caption/t Byte-level structure of extended C-APDUs.
\endinsert

\leavevmode

\nl A R-APDU may contain {\sbf response data} and is always terminated by a two-byte {\sbf status word} (SW1, SW2), indicating the result of the operation.

\midinsert
\clabel[table-rapdu-general]{R-APDU structure}
\ctable{c|c|c}{
\hfil Data & SW1 & SW2 \crl \tskip5pt
(optional) response data & \multispan2 status word \crl
}
\caption/t General structure of a R-APDU.
\endinsert

\seccc[apdu-communication-example] APDU Communication Example

An example of APDU communication can be seen in Figure \ref[img-fido-select].

\midinsert
\clabel[img-fido-select]{FIDO Select Log}
\picheight = 25mm \cinspic ../images/nfc-fido-select-cmd.png
\caption /f Device log of processing an APDU and selecting the FIDO applet.
\endinsert

\leavevmode

\begtt
C-APDU: 00  A4  04 00 08 A0 00 00 06 47 2F 00 01
        |   |   |  |  |  \_______ Data ________/
       CLA INS P1 P2 Lc

R-APDU: 46 49 44 4F 5F 32 5F 30   90 00
        \_______ Data ________/ \SW1 SW2/
\endtt

\leavevmode

\nl In this example, we can see the byte structure of a C-APDU, sent by a PCD, followed by an R-APDU, sent by a PICC. The C-APDU consists of the following fields: CLA~=~\code{0x00} (interindustry class byte - a standard entry point for commands), INS~=~\code{0xA4} (SELECT instruction), P1~=~\code{0x04} (selection by name) and P2~=~\code{0x00}. The field Lc~=~\code{0x08} specifies the length of the data field, which contains the 8-byte FIDO {\sbf application identifier (AID)} (\code{A0 00 00 06 47 2F 00 01}). After processing this message, the device responds with an R-APDU.

\nl The R-APDU contains the response data followed by the status word. The data field corresponds to the ASCII string ``FIDO\_2\_0'', while the status word~\code{0x9000} indicates successful execution of the command.

\seccc[nfc-chaining-difference] Chaining Mechanisms

Within ISO-DEP, APDUs are transported using I-Blocks. Since the maximum size of a single ISO-DEP frame is limited by the negotiated frame size, APDUs exceeding this size must be fragmented. This is achieved through {\sbf ISO-DEP chaining}, where a single APDU is split across multiple I-Blocks and reassembled by the receiving side.

\nl In addition to transport-level fragmentation, large responses may also be handled at the application level. In the case of short APDUs, the response length is limited by the value of the Le field. If the response exceeds this limit, it can be delivered in multiple steps using the status word mechanism (e.g., SW1~=~\code{0x61}), where the client issues the GET RESPONSE command to retrieve the remaining data.

\nl The two chaining mechanisms operate at different layers: ISO-DEP chaining ensures reliable transmission of a single APDU over multiple frames, while application-level response handling enables large response data to be retrieved through multiple APDU commands, especially when short APDU length fields are used.

\seccc[nfc-ndef] NDEF

The {\sbf NFC Data Exchange Format (NDEF)} is a standardized binary message format used to encode and exchange application-level data between NFC devices and tags. NDEF operates above the transport and protocol layers and is independent of the underlying RF technology.

\nl An {\sbf NDEF Message} consists of one or more {\sbf NDEF Records}. Each of those records consists of a 6-9 bytes long header and a payload. The type of the record is identified by the {\sbf Type Name Format (TNF)} field on the first byte of the header. TNF determines how the payload should be interpreted. Payload types include plain text, URIs, MIME-typed data (i.e. images), and application-defined external types identified using a domain-name-based naming convention. NDEF is widely used in the reader/writer mode and is broadly supported across NFC-capable devices and mobile operating systems.

\nl Due to its widespread adoption as a standard mechanism for application-level NFC data exchange, NDEF can be considered a fundamental component of practical NFC interoperability. Therefore, in order to provide a more complete implementation of basic NFC functionality as given by the guidelines, we also made basic NDEF support a part of the implementation scope alongside CTAP communication.

\nl In this implementation, we access NDEF records via ISO-DEP using APDU-based communication. The communication is as follows: The reader first selects the NDEF application using its AID, then accesses the {\sbf Capability Container (CC)} file to determine the tag's capabilities and supported data sizes. Finally, the reader selects the NDEF data file itself and performs read or write operations.

\nl APDU commands for those operations are:

\begitems
* {\sbf SELECT} (by AID or file identifier)
\nl Selects the NDEF application or a specific file within it.

* {\sbf READ BINARY}
\nl Reads the contents of the currently selected file, typically used to retrieve the NDEF message.

* {\sbf UPDATE BINARY}
\nl Writes data into the currently selected file, allowing the stored NDEF message to be modified if write access is permitted.
\enditems

\secc[nfc-nfc-dep] NFC-DEP

The {\sbf NFC-DEP} (NFC Data Exchange Protocol), defined in~\cite[iso18092], is designed for peer-to-peer communication between two {\sbf active} NFC devices. Unlike ISO-DEP, which follows a reader–card model, NFC-DEP enables symmetric communication, allowing both devices to alternate between initiator and target roles.

\nl In contrast to ISO-DEP, NFC-DEP {\sbf is not based on the APDU command–response model} used by ISO-DEP. Instead, communication is based on {\sbf message-oriented data exchange}, making it more suitable for peer-to-peer scenarios rather than card emulation.

\nl Since CTAP over NFC relies on APDU communication over ISO-DEP, NFC-DEP is outside the scope of this implementation.




